// -  Create a function called `factorio`
//    that returns it's input's factorial

function factorio(factorial: number) :number {
    let baseNum: number = 0;
    for(let i: number = 0; i < factorial; i++) {
        baseNum+= i;
    }
}