// -  Create a variable named `numList` with the following content: `[1, 2, 3, 4, 5]`
// -  Increment the third element simply by accessing it
// -  Log the third element to the console

let numList:number[] = [1, 2, 3, 4, 5];
//let incNum: number = 0;
//incNum += numList[2]++; -- It's weird atm that why did the stuff incremented my value if I didn't even refer to it.
console.log(numList[2]+1);